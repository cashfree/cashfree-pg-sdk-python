# coding: utf-8

"""
Cashfree Payment Gateway APIs

Cashfree's Payment Gateway APIs provide developers with a streamlined pathway to integrate advanced payment processing capabilities into their applications, platforms and websites.

The version of the OpenAPI document: 2026-01-01
Contact: developers@cashfree.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import date, datetime


from pydantic import validate_arguments, ValidationError, Field, StrictStr, StrictBytes, StrictInt, StrictFloat, StrictBool, field_validator, BaseModel
from typing import Any, Dict, List, Optional, Tuple, Union
from typing_extensions import Annotated

class PARRequest(BaseModel):
    """
    PARRequest
    """
    card_number: StrictStr = Field(description="The card number, containing 15 to 19 numeric digits without spaces or special characters.")
    card_cvv: StrictStr = Field(description="The Card Verification Value (CVV), a 3-digit code for most cards (Visa, Mastercard) and a 4-digit code for American Express, used for transaction authentication.")
    card_expiry_mm: StrictStr = Field(description="The two-digit expiry month (01-12), indicating when the card will expire.")
    card_expiry_yy: StrictStr = Field(description="The two-digit expiry year, representing the last two digits of the card’s expiration year.")
    card_type: Optional[StrictStr] = Field(default=None, description="Specifies the type of card, with the only accepted value being `PLAIN_CARD`.")
    __properties = ["card_number", "card_cvv", "card_expiry_mm", "card_expiry_yy", "card_type"]

    @field_validator('card_type')
    def card_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        if value not in ('PLAIN_CARD'):
            raise ValueError("must be one of enum values ('PLAIN_CARD')")
        return value

    # Updated to Pydantic v2
    """Pydantic configuration"""
    model_config = {
        "populate_by_name": True,
        "validate_assignment": True
    }

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> PARRequest:
        """Create an instance of PARRequest from a JSON string"""
        return cls.from_dict(json.loads(json_str))
    
    @classmethod
    def from_json_for_one_of(cls, json_str: str) -> PARRequest:
        """Create an instance of PARRequest from a JSON string"""
        temp_dict = json.loads(json_str)
        if "card_number, card_cvv, card_expiry_mm, card_expiry_yy, card_type" in temp_dict.keys():
            return cls.from_dict(json.loads(json_str))
        return None

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(by_alias=True,
                          exclude={
                          },
                          exclude_none=True)
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> PARRequest:
        """Create an instance of PARRequest from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return PARRequest.parse_obj(obj)

        _obj = PARRequest.parse_obj({
            "card_number": obj.get("card_number"),
            "card_cvv": obj.get("card_cvv"),
            "card_expiry_mm": obj.get("card_expiry_mm"),
            "card_expiry_yy": obj.get("card_expiry_yy"),
            "card_type": obj.get("card_type")
        })
        return _obj


# Pydantic v2: resolve forward references & Annotated types
PARRequest.model_rebuild()

