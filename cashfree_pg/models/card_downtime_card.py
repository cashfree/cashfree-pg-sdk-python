# coding: utf-8

"""
Cashfree Payment Gateway APIs

Cashfree's Payment Gateway APIs provide developers with a streamlined pathway to integrate advanced payment processing capabilities into their applications, platforms and websites.

The version of the OpenAPI document: 2026-01-01
Contact: developers@cashfree.com
Generated by OpenAPI Generator (https://openapi-generator.tech)

Do not edit the class manually.
"""  # noqa: E501

from __future__ import annotations
import pprint
import re  # noqa: F401
import json

from datetime import date, datetime


from pydantic import validate_arguments, ValidationError, Field, StrictStr, StrictBytes, StrictInt, StrictFloat, StrictBool, field_validator, BaseModel
from typing import Any, Dict, List, Optional, Tuple, Union
from typing_extensions import Annotated

class CardDowntimeCard(BaseModel):
    """
    CardDowntimeCard
    """
    card_issuer: Optional[List[StrictStr]] = None
    card_type: Optional[List[StrictStr]] = None
    card_network: Optional[List[StrictStr]] = None
    __properties = ["card_issuer", "card_type", "card_network"]

    @field_validator('card_type')
    def card_type_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in ():
                raise ValueError("each list item must be one of ()")
        return value

    @field_validator('card_network')
    def card_network_validate_enum(cls, value):
        """Validates the enum"""
        if value is None:
            return value

        for i in value:
            if i not in ():
                raise ValueError("each list item must be one of ()")
        return value

    # Updated to Pydantic v2
    """Pydantic configuration"""
    model_config = {
        "populate_by_name": True,
        "validate_assignment": True
    }

    def to_str(self) -> str:
        """Returns the string representation of the model using alias"""
        return pprint.pformat(self.dict(by_alias=True))

    def to_json(self) -> str:
        """Returns the JSON representation of the model using alias"""
        return json.dumps(self.to_dict())

    @classmethod
    def from_json(cls, json_str: str) -> CardDowntimeCard:
        """Create an instance of CardDowntimeCard from a JSON string"""
        return cls.from_dict(json.loads(json_str))
    
    @classmethod
    def from_json_for_one_of(cls, json_str: str) -> CardDowntimeCard:
        """Create an instance of CardDowntimeCard from a JSON string"""
        temp_dict = json.loads(json_str)
        if "card_issuer, card_type, card_network" in temp_dict.keys():
            return cls.from_dict(json.loads(json_str))
        return None

    def to_dict(self):
        """Returns the dictionary representation of the model using alias"""
        _dict = self.dict(by_alias=True,
                          exclude={
                          },
                          exclude_none=True)
        return _dict

    @classmethod
    def from_dict(cls, obj: dict) -> CardDowntimeCard:
        """Create an instance of CardDowntimeCard from a dict"""
        if obj is None:
            return None

        if not isinstance(obj, dict):
            return CardDowntimeCard.parse_obj(obj)

        _obj = CardDowntimeCard.parse_obj({
            "card_issuer": obj.get("card_issuer"),
            "card_type": obj.get("card_type"),
            "card_network": obj.get("card_network")
        })
        return _obj


# Pydantic v2: resolve forward references & Annotated types
CardDowntimeCard.model_rebuild()

